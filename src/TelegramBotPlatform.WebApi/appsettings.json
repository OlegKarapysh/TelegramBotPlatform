{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "Platform": {
    "AdminApiKey": "take from user secrets",
    "PluginsDirectory": "plugins",
    "WebhookBaseUrl": "take from user secrets or env in production; each bot's webhook is {WebhookBaseUrl}/{botId}"
  },
  "Persistence": {
    "ConnectionString": "take from user secrets"
  }
}
